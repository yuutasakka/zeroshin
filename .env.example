# ===================================
# 環境変数の例（実際の値は入れないでください）
# ===================================

# Supabase設定
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Twilio設定（Vercel環境変数でのみ設定）
# TWILIO_ACCOUNT_SID=（Vercelの環境変数で設定）
# TWILIO_AUTH_TOKEN=（Vercelの環境変数で設定）
# TWILIO_PHONE_NUMBER=（Vercelの環境変数で設定）

# セキュリティキー（自動生成される - Vercel環境変数でのみ設定）
# ENCRYPTION_KEY=（Vercelの環境変数で設定）
# JWT_SECRET=（Vercelの環境変数で設定）
# SESSION_SECRET=（Vercelの環境変数で設定）
# CSRF_SECRET=（Vercelの環境変数で設定）

# 管理者設定（Vercel環境変数でのみ設定）
# ADMIN_USERNAME=（Vercelの環境変数で設定）
# ADMIN_PASSWORD_HASH=（Vercelの環境変数で設定）

# ===================================
# 重要な注意事項：
# 1. このファイルは例として使用してください
# 2. 実際の値は絶対にコミットしないでください
# 3. 本番環境の値はVercelの環境変数に直接設定してください
# 4. ローカル開発では.env.localファイルを作成してください（gitignoreされています）
# ===================================